# Minimal original-PinePhone session.  wvkbd reserves the lower screen region.
set $root /usr/local/share/emacsos-openrc
set $power $root/session-power

xwayland disable
swaybg_command /bin/true
default_border none
default_floating_border none
focus_follows_mouse no
mouse_warping none
workspace_layout tabbed

output DSI-1 mode 720x1440@60Hz scale 2
input type:touch map_to_output DSI-1
seat * hide_cursor 1000

bindsym XF86PowerOff exec $power press
# Consumed by Sway: resets idle and triggers its paired resume finalizer.
bindsym F24 nop
# Volume keys are the only other handset seat activity and clear idle origin.
bindsym XF86AudioRaiseVolume exec $power wake
bindsym XF86AudioLowerVolume exec $power wake
bindsym Mod1+Tab focus next

for_window [app_id="emacs"] fullscreen enable
for_window [app_id="firefox"] focus
