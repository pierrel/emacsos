#+title: EmacsOS Roadmap
#+date: 2026-05-17

Each experiment is its own design doc + branch (see =docs/= for
landed ones).  This file collects forward-looking work that
doesn't fit any specific experiment yet — usability iteration,
cross-cutting concerns, follow-ups discovered live on the phone.

* IN FLIGHT: Voice-call assistant (paused at P3 power gate)

*The big current initiative* — dial the SIM and talk to assist by voice: a
PIN-gated *receptionist* agent helps you reach a thread (or start one with a
domain), then a natural voice conversation inside it. It sidesteps the iOS-Safari
background-audio blocker entirely (a real call; the OS owns it) and builds on the
*already-validated* SIM7600G-H telephony substrate.

*Design and live handoff:*
- PRD: [[file:docs/2026-07-21-voice-call-assistant.org][docs/2026-07-21-voice-call-assistant.org]]
- Tech design (six-lens-reviewed + Pierre round-2): [[file:docs/2026-07-21-voice-call-tech-design.org][docs/2026-07-21-voice-call-tech-design.org]]
- P3 implementation and stopping point: [[file:docs/2026-07-28-voice-call-p3-bridge.org][docs/2026-07-28-voice-call-p3-bridge.org]]
- PR #33 merged the design.  The receptionist/catalog was reviewed in closed
  assist PR #208 and later landed on =main=; assist PRs #209/#211/#214/#219/#220
  merged and deployed Flow, Speech, Wire, Session/ordinary-Run integration,
  PIN/caller security, and scripted reconstruction.
- Emacsos PR #34 is the landed and deployed phone-side bridge.  It owns primary
  ttyUSB2 for AT/audio and ttyUSB4 for PCM; ttyUSB3 stays with the human UI and
  ModemManager keeps QMI call detection/data.

*Phasing:*
- *P0/P0.5/P1* [assist] — *DONE + DEPLOYED.* Durable ordinary/child Runs and
  terminal observers are authoritative; do not revive the removed
  =plan_turn=/=route_turn= or backlog submission paths.  The complete fake-bridge
  path includes receptionist, audio detectors, speech, transport, Session,
  ordinary-Run delivery, global PIN lockout, caller normalization, detector
  scoping, and reconstructable call logs.
- *P2* [assist] — async delivery and both go-back paths remain a follow-up after
  the basic live call is proven.  They are not part of the immediate restart
  checklist.
- *P3* [emacsos + assist] — *IMPLEMENTED; LIVE VALIDATION PAUSED ON POWER.* The
  bridge sees inbound calls and reaches Assist when voltage is stable.  Each recent
  ring coincided with Pi undervoltage; on 2026-07-29 it lasted through both WSS
  attempts and, on an earlier call, through the picked-up-but-silent interval.
  Resume only after replacing the power path and proving no undervoltage during an
  actual ring and sustained call.  Then validate PIN audio/DTMF, TTS downlink,
  caller uplink/STT, clock rate, and barge-in.  Pierre initiates every live call.

*Depends on / subsumes existing roadmap items:* the [[*TODO Finish stable-power live validation of full-duplex call-audio bridge][full-duplex call-audio bridge]]
(P3, the one hardware-gated piece), [[*TODO Speech to text][Speech to text]] (STT endpoint), and the shipped
[[*DONE Phone calling][Phone calling]] / [[*DONE Phone answering][Phone answering]] primitives. Companion validated-on-device
facts: [[file:docs/2026-06-18-call-audio.org][2026-06-18-call-audio.org]], [[file:docs/2026-06-18-inbound-answering.org][2026-06-18-inbound-answering.org]].

*Status 2026-07-29:* the basic P0/P1 live-call server path and Emacsos PR #34's
phone bridge are merged and deployed; P2 remains after basic live validation.
The next action is the physical power fix and stable-power live checklist in its
state doc, not another software slice.

* TODO PinePhone minimal client with reliable deep sleep

Use the PinePhone as the first integrated Assist smartphone testbed, but replace
the general-purpose Phosh desktop with the smallest useful awake surface: a
minimal Wayland compositor, PGTK Emacs, phone primitives, and the Assist client.
Firefox and Waydroid make a compositor non-optional; they run as on-demand
full-screen clients rather than turning EmacsOS back into a desktop.  Removing
the Phosh/GNOME shell should improve responsiveness and reduce awake overhead,
but it is explicitly *not* the battery strategy by itself.

Battery life depends on making Crust deep suspend reliable.  The intended power
model is:
- while awake, run only the minimal Emacs UI and services required for phone
  operation;
- turn the display off automatically and enter deep suspend when idle;
- leave the cellular modem registered, with its ring indicator able to wake the
  application processor for incoming calls and SMS;
- keep canonical Assist execution on the always-on server, then reconnect the
  phone client after wake instead of keeping the phone CPU and a network socket
  awake continuously;
- treat calls, SMS, and alarms as wake-capable.  Ordinary internet notifications
  are not assumed to arrive during deep sleep; any Assist-initiated urgent wake
  needs an explicit call/SMS path or must wait for the next wake.

Field evidence from postmarketOS edge on the PinePhone 1.2 (2026-08-30): with
automatic display blanking disabled for diagnosis and the phone kept awake, the
battery reported about 1.04 A at 3.87 V (roughly 4 W), consistent with only a few
hours of runtime.  A separate failure occurred after Phoc turned the display off:
the phone stopped logging and became unreachable, with no recorded suspend,
power-key event, kernel panic, GPU error, OOM, or clean shutdown.  Do not paper
over that failure with a lighter UI; prove the new display-off and suspend/resume
path directly.

The v26.06/OpenRC control captured a kernel RCU stall after USB power removal,
so it is rejected for this experiment.  The active v25.06/OpenRC/6.13.8 image
has passed a multi-hour plugged run with Sway, PGTK Emacs, wvkbd, handset audio,
Firefox, on-demand Waydroid, repeated DPMS, and intentional UI failure recovery.
Pierre's first unplugged hands-on run, physical display-off/wake, and an
RTC-driven deep suspend passed with the same Sway and Emacs processes.  During
unattended integration, only the reviewed 60-second display blank is installed;
the kernel-suspend helper and UI suspend privilege are removed so the phone
remains remotely recoverable.  Four
plugged automatic RTC-wake cycles passed under the former suspend policy.  The
three post-fix captures were clean, including a 23-minute-33-second unreachable
interval between kernel suspend entry and RTC wake, followed by visible Firefox
and Android browser smokes.  The next gates are duration and battery runs,
followed by modem validation with Pierre present.
Firefox/Android authority
separation and restrictive guest networking remain gates before untrusted
Android apps.  Detailed evidence and rejected
hypotheses are in [[file:docs/2026-08-31-pinephone-emacsos-session.org][the session record]];
verified images and fallback procedure are in
[[file:docs/2026-09-01-pinephone-base-recovery.org][the base-recovery record]].

Acceptance is repeated unplugged display-off/deep-suspend/resume cycles with
working calls and SMS wake, clean modem and network recovery, Assist reconnection,
and materially lower measured standby drain.  Also test the charging case
separately because PinePhone USB-C/display interactions have already produced
different behavior in live use.

The staged direct-computer architecture, automatic screenshot/input/suspend
harness, authenticated lab recovery, later locked fallback, promotion gates,
and stop conditions are in
[[file:docs/2026-08-31-pinephone-emacsos-session.org][2026-08-31-pinephone-emacsos-session.org]].

* TODO Reach Assist Web functionality parity on the phone

This is a source-grounded inventory and path to phone access to the functions
currently offered by Assist Web. The baseline is Assist =2d8ec13a= and EmacsOS
=7b2ccf1= (2026-09-24). Parity means the same canonical Assist thread and
server-owned decisions, presented in a form usable on the PinePhone. It does
not require copying browser layout or running a second agent on the phone.
Keep this inventory current as either project changes. The stages below are
ordered by user impact; each remaining function stays visible until completed
or a specific later decision changes its disposition.

** Already implemented or partly implemented

- *Canonical threads and runs: implemented.* The phone has an authenticated
  HTTPS Assist API, canonical thread list and chooser, repository and harness
  choice, create/send, recent snapshot and older history, finite SSE Run
  observation and cancellation. It preserves local drafts, bounded caches,
  idempotency keys, pending work and recovery across interruption. It renders
  cached content as cached rather than claiming it is live. See
  =README.org:207-238=, =assist-web.el:417-478=,
  =assist-web.el:1520-1621=, =assist-web.el:1815-1924=,
  =assist-web.el:2119-2308=, =assist-web.el:2919-3025= and
  =assist/manage/web/phone_api.py:1103-1293=. These contracts are the base for
  later parity work, not features to rebuild.
- *List search and repository context: partly implemented.* Web filters the
  already loaded list by its displayed title (which can be a pending-message
  snippet while busy) and shows status pills plus a Pi-preview
  or engine-error label (=assist/manage/web/threads.py:261-468=); repository
  and domain context appear on the thread page. Phone completion searches
  description, normalized title, repository and status; its touch list shows
  title, repository and raw status (=assist-web.el:1531-1579=,
  =assist-web.el:2119-2230=). New-thread repository and harness selection
  works (=assist-web.el:2919-3011=). The phone list has no native in-list
  search with a visible match count or no-match state, no engine label,
  and none of Web's attention badges.
- *Thread content: partly implemented.* Assist's phone snapshot already
  supplies each visible message's =kind= and optional timestamp, plus
  thread-level workspace metadata; safe file references are included only
  for eligible ordinary assistant replies
  (=assist/manage/web/phone_api.py:535-619=); the
  EmacsOS validator retains only id, role, text and state, then renders a
  text transcript with basic Markdown links (=assist-web.el:524-585=,
  =assist-web.el:1815-1912=, =chat.el:340-489=). Assist also offers bounded
  diff, manifest and regular-file archive APIs, but the native client does
  not use them (=assist/manage/web/phone_api.py:1002-1100=,
  =assist/manage/web/phone_api.py:1295-1310=).

** P1: Match Web list status, urgency and unread behavior

The Web list ranks urgent first, then active work except queued, then queued,
then unread/new, then idle; activity age breaks ties. Its visible pill priority
is queued, active stage, error, urgent, new, unmerged. A continuation uses
"following up" as its active label and keeps a simultaneous "new" pill when
the first answer is unread. Pi preview and engine error are independent
engine labels. =awaiting_approval= has no separate list pill; the unread
signal and the in-thread approval card surface it. See
=assist/manage/web/threads.py:243-357= and
=assist/manage/web/state.py:405-441=.

Assist's catalog already returns stage, unread, activity time and Web-ranked
rows, but it omits urgent and unmerged; on a thread-engine error it skips the
row altogether (=assist/manage/web/phone_api.py:633-660=). EmacsOS drops the
unread and activity fields, keeps only the raw stage and repository, and does
not draw any pill (=assist-web.el:432-478=,
=assist-web.el:2119-2274=). Extend the authenticated catalog with the
missing server-owned signals and an engine-error representation. Preserve the
server's row order in both touch list and chooser. Show all the pill cases
above, including urgent rank when a live-state pill masks the urgent pill, and
show stale/cache state separately from thread status. The current API slices
=MANAGER.list()[:MAX_THREADS]= before applying attention rank, so canonical
threads beyond the first 500 are undiscoverable through the catalog and
chooser, including older urgent or unread threads
(=assist/manage/web/phone_api.py:633-655=). They remain accessible when the
caller already knows their IDs; the current list offers no way to discover
those IDs. Provide bounded pagination or server-ranked search without an
unbounded client catalog. Page traversal must remain stable or recover when
attention ranking changes between pages, including after restart. Every
canonical thread must be discoverable, with urgent/unread precedence tested
separately. The existing README's claim that both entry points share the
"same complete ... catalog" (=README.org:216=) is stale because of this cap.
Add a discoverable search action in the touch list itself, with a visible
match count and explicit no-match result, matching Web's list search feedback
(=assist/manage/web/threads.py:433-466=).

The current server marks unseen on *every* transition to =ready= or
=awaiting_approval=, including cancellation paths that produce no answer.
Ordinary errors do not mark it, but continuation failures explicitly mark
unseen while retaining the higher-priority error status. This differs from
the intended "unseen response" meaning and must be reconciled in the server
before the phone relies on the bit as a response badge
(=assist/manage/web/state.py:459-467=,
=assist/manage/web/threads.py:1619-1634=,
=assist/manage/web/threads.py:3120-3131=,
=assist/manage/web/phone_api.py:966-973=). The agent's =notify()= marks a
separate stronger urgent bit. Both bits have best-effort restart markers, and
Web clears both when its thread page opens (=assist/manage/web/state.py:512-619=,
=assist/manage/web/threads.py:3316-3329=). Marker-file writes can fail, so
attention bits normally restore across restart but reliable persistence remains
an acceptance requirement. A phone snapshot GET currently clears neither
(=assist/manage/web/phone_api.py:535-619=). Give the phone an authoritative
seen acknowledgment tied to a freshly displayed canonical
snapshot. Cached/offline display must never clear server attention, and an
acknowledgment of an older view must not erase a later reply or urgent mark.
Refresh the catalog on list entry and after wake; the phone must report when
status is cached or stale. A compact global Assist indicator can summarize
known attention, while the thread rows remain the detailed source; fit it
after the existing call and SMS badges on the small modeline
(=os.el:96-127=). Do not promise instant Internet notifications while the
PinePhone is in deep sleep; its power roadmap explicitly requires an SMS/call
wake path or waiting for the next wake (=roadmap.org:57-76=).

Acceptance: queued/processing/paused/setup/error/urgent/new/unmerged,
following-up-plus-new, ordinary successful =ready= unseen marking, unseen
continuation failure with its visible error pill, Pi preview and engine-error
cases render with Web precedence and ordering. Unread and urgent reliably
survive service and phone restart, clear after a fresh opened view, and remain
when only cached content was shown or a newer response arrived before
acknowledgment. An urgent thread still sorts first when its visible pill
describes current processing. Every canonical thread beyond the first catalog
page remains reachable by list navigation or search. Touch-list search reports
a matching count or a clear no-match state.

** P2: Complete conversation and decision flows

Render the snapshot's message =id= and =kind=, which distinguish background
continuations, tasks and mid-turn interjections, plus its optional timestamp
and filtered safe file references. Add bounded API projections where absent
for collapsed tool/activity trace, completed-turn duration, pending
continuation and approval details, without treating provisional SSE text as
canonical.
Web already renders these distinctions, timing badges, pending messages,
errors and approval cards (=assist/manage/web/threads.py:760-787=,
=assist/manage/web/threads.py:867-1097=); the phone currently has only
generic status and user/assistant text (=assist-web.el:1815-1912=). Give a
failed setup or turn a safe, actionable explanation and recovery path: Web
shows the actual error banner, while the phone snapshot currently replaces
it with the same generic "Thread failed; inspect Assist Web for details"
message (=assist/manage/web/threads.py:1019-1030=,
=assist/manage/web/phone_api.py:603-613=). Do not expose secrets or an
unbounded traceback in the phone response.

Add phone actions for rename, delete with a native two-tap confirmation,
and copy/view thread ID; show harness, domain and Pi availability. For a
Pi preview thread, preserve read-only behavior when that preview is
disabled and offer its explicit "Continue in Deep Agents" flow with an
optional summary and a clear warning that neither its transcript nor its
workspace transfers. Web already has these controls (=assist/manage/web/threads.py:720-744=,
=assist/manage/web/threads.py:1207-1234=,
=assist/manage/web/threads.py:1337-1373=,
=assist/manage/web/threads.py:4459-4470=,
=assist/manage/web/threads.py:5091-5136=); the native action set currently
contains send, abort, refresh, older history, open-object and catalog only
(=assist-web.el:3013-3025=).

Make decisions that stop a thread actionable on the phone: approve, edit or
reject SMS replies and email; approve for one hour, allow for the thread or
decline an exact host/port egress request; approve or decline geo downloads
and show import/failure progress. Each compact card must show the material
target, content and scope before action: SMS recipient and full proposed
reply; email sender, recipients, subject and body; exact egress host/port,
reason and duration/scope; geo region, size and download effect. After an
action, show its accepted, rejected, stale or error result in the same
thread context on the 320x240 display. Reuse server authority and
stale-decision checks; do not let cached cards authorize an obsolete request.
These Web cards and routes exist at =assist/manage/web/threads.py:1031-1205=,
=assist/manage/web/threads.py:3709-3769=,
=assist/manage/web/geo.py:90-144= and
=assist/manage/web/egress.py:87-118=; the current phone API has no
decision route (=assist/manage/web/phone_api.py:1103-1310=).

Acceptance: a phone user can read an agent answer and its origin, recover
an interrupted Run, identify and act on every pending thread decision,
rename/delete a thread intentionally, and distinguish a disabled Pi preview
from a failing ordinary thread. Setup and turn errors give safe next actions
on the phone. Approval cards disclose the exact decision and show its
outcome in place. Refreshed state supersedes any stale card.

** P3: Open thread work and rich responses

Offer phone-sized access to the thread's repository/branch/dirty state,
file references, safe workspace files and =render= content (Org, Markdown,
images and PDF), map blocks, diff/conflict state, and local pinned replies.
Web currently renders those file/map blocks and full-page views and has a
browser-local pin list (=assist/manage/web/threads.py:903-937=,
=assist/manage/web/threads.py:1498-1550=,
=assist/manage/web/threads.py:4581-5090=). Its diff, review form and
Merge & Push flow are at =assist/manage/web/threads.py:809-865=,
=assist/manage/web/review.py:352-386= and
=assist/manage/web/threads.py:5223-5287=. Add phone review annotations
and merge/push controls through explicit authenticated server contracts,
showing merge conflict details and requiring a native confirmation for
destructive effects. Establish shared per-thread write exclusion for canonical
Runs and merge/push before comparing the current branch with
the exact revision/tree the server reviewed and the phone displayed, including
the full diff and untracked files. Keep it through the merge/push effect, and
wait for or reject while another writer is active. If content changed, reject
the stale action, refresh the review, and require new confirmation; matching
the branch name alone is insufficient. A phone send should supply appropriate
local time zone and location context when authorized, matching the browser's
context rider
(=assist/manage/web/threads.py:183-187=); the current phone send body carries
only message text (=assist/manage/web/phone_api.py:122-130=).

The separate =emacsos-thread-git-sync= effort has implementation in flight
under its accepted local design; it has no linkable in-repo design or PR yet.
That design specifies a clean, committed, thread-specific branch, a separate
Git credential, and device =git fetch= after a successful canonical terminal
snapshot (without merging). It calls for =C-x C-f= browsing and thread-local
=C-c d= Magit against a labeled, remote-published =main=, while preserving
terminal =C-c d= elsewhere. This is separate-effort design scope, not shipped
phone behavior.
This Git-sync path does not itself provide Web diff, dirty-worktree, or
untracked-file parity.
The present phone archive excludes =.git= and provides bounded regular files;
the diff endpoint is bounded text, not a full Git synchronization protocol
(=assist/manage/web/phone_api.py:339-391=,
=assist/manage/web/phone_api.py:1002-1100=). Its current =git diff main...=
projection omits untracked files, whereas Web's =main_diff= includes them
as additions (=assist/domain_manager.py:205-265=). P1 and P2 proceed
independently. At P3 implementation/integration, reconcile this roadmap with
that effort's eventual reviewed implementation and avoid a second competing
mirror or diff path.

Web's render pages can also expose artifacts saved under a thread's sandbox
=/tmp= mount. Provide authenticated, bounded access to those per-thread
artifacts, including PDF, as well as workspace files; the current phone
manifest and archive cover only the workspace
(=assist/manage/web/threads.py:4518-4531=,
=assist/manage/web/threads.py:4717-4725=,
=assist/manage/web/phone_api.py:1002-1100=,
=assist/manage/web/phone_api.py:1295-1310=).

Acceptance: exact thread file and diff actions open safely on the phone,
review/merge decisions act on the same canonical branch shown to the user,
and diff changed-file/content coverage matches Web, including untracked
files, with bounded/truncated content labeled rather than presented as complete.
Map, workspace and sandbox =/tmp= artifact content, including PDF, has a
usable phone view or a safe explicit external-view action. Merge & Push is
rejected whenever the reviewed revision/tree or full diff changes before the
atomic operation against Run and merge writers. Thread-local Git navigation
follows the accepted Git-sync contract when it lands, while Web diff parity
still covers dirty and untracked work.

** P4: Make remaining Web operator and evaluation functions reachable

These are lower-priority phone milestones, not silent exclusions. They
require compact phone views and additional authenticated API contracts:

- Scheduled prompts: show each item's owning thread, prompt, cadence,
  paused state and next fire before offering deletion
  (=assist/manage/web/schedules.py:22-44=,
  =assist/manage/web/schedules.py:65-77=).
- Global egress and geo management: show live egress grants and support
  revocation; pending egress approvals remain in-thread. The geo page lists
  regions and pending proposals, with approve, decline, deletion and transit
  actions (=assist/manage/web/egress.py:87-118=,
  =assist/manage/web/geo.py:87-144=). These global controls complement,
  rather than replace, P2's in-thread egress cards.
- Judged live captures: capture recent or full visible conversation, record
  pass/fail calibration, inspect and dismiss capture cards, preserving the
  distinction between private stored evidence and browser-local dismissal
  (=assist/manage/web/threads.py:1390-1420=,
  =assist/manage/web/threads.py:5181-5221=,
  =assist/README.md:445-449=). This is an operator workflow, so it follows
  core chat and approval use.
- Eval history and run detail: read the existing evaluation pages through
  a phone-sized view (=assist/manage/web/evals.py:251-256=). This is a
  diagnostic view rather than a prerequisite for a conversation.
- Pi preview activity trace: expose its bounded, redacted diagnostic events
  without implying they are agent messages (=assist/README.md:89-97=,
  =assist/manage/web/threads.py:487-510=). Keep its diagnostic character.

Assist's inbound SMS route and =notify()= outbound urgent SMS are server
transports, already shared rather than phone-client controls
(=assist/manage/web/threads.py:3661-3689=,
=assist/README.md:429-436=). P1 covers the phone's display of their
resulting attention state. Browser favicon and Apple touch icon routes are
browser chrome, with no native phone action to reproduce
(=assist/manage/web/threads.py:3207-3223=). These are the only intentionally
phone-unsuitable entries in this inventory; they remain recorded here so
the comparison is explicit.

** Verification and closure

For each milestone, update this section's =implemented=, =partial= or
=missing= status for both the phone API and native client, and refresh the
source revision. Tests must prove badge rank/display independently, unread
and urgent acknowledgment across cached/live/restart/newer-result cases,
protected stale-decision rejection, serialized stale-merge rejection, and
safe/truncated file, artifact and Git views. Run ERT and Assist API tests
for each implemented slice, then exercise
the list, one normal and one Pi thread, an interrupted Run, approval cards,
file/diff views and wake/reconnect on the PinePhone or simulator. A milestone
is complete only when the live phone can perform its named flow against the
canonical Assist state.

* Ergonomics iteration

Live testing of PR #6 (assist-import) surfaced several rough edges
in the phone UX that aren't load-bearing for any specific
experiment but compound across all of them.  Worth a dedicated
pass.

** DONE Ctrl/Meta modifier keys

Shipped on the =modifier-keys= branch: a leftmost MOD button on the
action row cycles =nil → C → M → C-M → nil=; under a non-nil
modifier, the T9 grid filters to letters that have a binding under
the current modifier in the target buffer's full active-keymap stack
(via =key-binding=), and a tap *fires* the bound command instead of
inserting a letter.  Single-letter fast-path (one bound letter →
fire on tap, no wait); multi-letter subset → arm-then-commit with
multi-tap cycling.  Sticky modifier (only an explicit MOD tap
changes it).  QUIT abandons an armed binding; every other utility
tap commits it.  Shift was rolled into the existing CAPS (locked
decision; under MOD, caps is ignored and canonical lowercase keys
go to the keymap walker).  Full design + decisions in
[[file:docs/2026-05-27-modifier-keys.org][2026-05-27-modifier-keys.org]].

** DONE Ordinary UI has no Emacs control pane

The original built-in T9 keyboard and later keyboard/command combination are
recorded in [[file:docs/2026-05-21-keyboard-ergonomics.org][the historical keyboard design]].  The PinePhone now uses the compositor
modifier-swipe keyboard, so ordinary chat, editing, and Assist buffers give the
whole Emacs area to content.  =Meta+x= reaches every interactive command; no
per-buffer command list or persistent utility pane remains.  A temporary
control window appears for call and SMS confirmations and for an SMS
conversation's QUIT/Refresh/Send-or-ACK row, then is deleted.
See [[file:docs/2026-09-08-remove-control-pane.org][the corrective design]].

** TODO Glide decoder improvements

Build a locally captured correction comparison set, then add a bounded frequency
prior, DTW/SHARK2 geometry, and context ranking through a separately reviewed
context seam. Privacy and storage need their own reviewed design first.

** DONE Tab button on the keyboard

Shipped in PR #11: TAB is the 4th button on the SPC/RET/DEL action row
(its own =action-w= so the letter keys don't shrink).  Context-aware —
=minibuffer-complete= in the minibuffer, else =indent-for-tab-command=.

** DONE Keyboard layout + sizing iterations

The Optimal-T9 layout from the Qin et al. paper is the v1
baseline.  Live use will surface things the paper doesn't capture
on this specific screen (gap proportions, scale factor between
letter and util buttons, fingertip-vs-stylus accuracy bias,
number row treatment, punctuation strategy).  Iterate based on
what's actually painful in real use rather than redesigning
preemptively.

** TODO Font choice + sizing

The default emacs face on the phone is whatever the system X
fontconfig defaults pick — not optimized for a 320x240 surface
held at arm's length.  Pick a font (and sizes for transcript /
input / keyboard / status) that reads well at this density:
legibility at small px counts, distinguishable lowercase l vs.
uppercase I vs. digit 1, comfortable line-height in =*chat*=.
Candidates: a programming font (Iosevka, JetBrains Mono) tuned
for clarity at small sizes, or a screen-optimized humanist like
Inter.  Try a couple, decide on the phone, codify in =os.el=
(or a per-face block in the init snippet) so phone-install
sets it consistently.

* Tool-call transparency

The emacs-modifying channel ships with `status: calling <tool_name>`
events for each new tool invocation.  Intended UX was richer
(`status: <tool_name>: <truncated args>`) but the implementation
needs `tool_call_chunks` accumulation — args generally arrive across
many continuation chunks, not on the first chunk where the tool
name lands.  Follow-up work: stitch `tool_call_chunks` per
tool_call_id, emit `<name>: <args>` when complete (or stream args
character-by-character — see below).

Other dimensions worth iterating once the basic args preview lands:
- *Tool-result visibility.*  Right now elisp results stay inside the
  langgraph trace; the user sees only the agent's prose summary
  afterwards.  For a device the agent is MODIFYING this may be too
  opaque — a `status: eval_elisp result: <preview>` event would let
  the user spot a wrong-direction call early.  Trade-off: noisier
  on multi-call sessions; might warrant a "verbose" toggle.
- *Streaming partial args.*  Show args character-by-character as
  they stream in (like the bot text already does).  Costs an event
  per chunk per arg; consider only for slow tool-args generation.

* Other / cross-cutting

** DONE Drop the deprecated =loop_exploration_tools= kwarg (SMALL — do next)

Done on branch =port-to-agentspec= (2026-06-11), folded into the AgentSpec
port: the kwarg and =LOOP_EXPLORATION_TOOLS= are gone from =_build_thread= /
=channel.py=.

assist rolled back its LoopDetectionMiddleware to only the two exact-repeat
patterns (same-tool-same-error / same-tool-same-args) and removed the
distinct-args "exploration breadth" pattern that =loop_exploration_tools= fed.
The kwarg is now a deprecated *no-op* on the assist side: =create_agent= /
=Thread= still ACCEPT it (so we don't break) but ignore it.

We still pass it:
- =server/emacsos_server/app.py:276= → =create_agent(loop_exploration_tools=LOOP_EXPLORATION_TOOLS, ...)=
- =LOOP_EXPLORATION_TOOLS = frozenset({"eval_elisp"})= at =server/emacsos_server/channel.py:298=
- imported at =app.py:34=

Action: drop the kwarg at the call site and remove =LOOP_EXPLORATION_TOOLS=
(and its import).  =eval_elisp= is unaffected — loop detection now applies the
exact-repeat guards uniformly to every tool, so there's nothing to tune per
tool.  Once emacsos stops passing it, assist can delete the no-op param
entirely (its docstrings already point here).

** DONE Conversation persistence across /chat requests

Shipped: =_build_thread= now persists the conversation across /chat turns
via a fixed thread id + a =SqliteSaver= checkpointer on
=<state_dir>/threads.db= — the Thread OBJECT is rebuilt each request, but
its state lives in the checkpointer (replayed by thread id), instead of
the old fresh-state-per-request — so the agent remembers prior turns
("now make it bigger", "make that permanent").  Reset is an explicit
"New chat" (=POST /clear= → =delete_thread= + a wipe of the conversation
working dir, which clears the agent's =AGENTS.md= memory too).  Context-window growth is
delegated to deepagents' =SummarizationMiddleware= (no custom windowing);
the raw log accumulates in threads.db but the model context stays
bounded, and New chat is the release valve.  See
[[file:docs/2026-05-22-conversation-history.org][docs/2026-05-22-conversation-history.org]].
Keyed by a single fixed id (one personal phone) — per-phone keying is
deferred with the WireGuard/away-phone work.

** DONE Config history and conversational snapshot restore

=config_history= lists saved versions and =revert_config(target)= restores a
selected version by applying its complete body and recording a new snapshot.
Without a target, =revert_config= and =M-x emacsos--chat-rollback= write the
nearest semantically distinct prior config and record it, providing the quick
"undo the thing I just did" path.  =get_config= reads the committed source of
truth even after =/clear=, so persistent changes do not depend on conversation
memory.

** TODO Selectively revert one historical config change

Snapshot restore replaces the complete config.  A future conversational path
could invert one selected past change while preserving compatible later
changes, with an honest conflict result when that cannot be done cleanly.

** DONE Phone tempdir leak

Fixed as a consequence of conversation persistence (above): the
per-/chat `tempfile.mkdtemp` is gone — the persistent conversation now
uses ONE stable working dir under `<state_dir>/threads/`, reused across
turns, so there's no per-turn directory to accumulate.

** DONE Sub-agent turns wedge the server (langgraph 1.2 streamed-path bugs)

Any /chat turn that spawned a SUB-AGENT (=task= → =research-agent=,
=context-agent=) wedged the moment the sub-agent ran: no next model call,
no error, just silence; then the orphaned worker wedged EVERY later turn
until the uvicorn worker was restarted.

*Real root cause* — a py-spy dump (2026-05-25) DISPROVED the original
shared-=SqliteSaver=-contention theory (kept at the bottom for the record).
Three distinct bugs, all from the langgraph 1.2.0 upgrade, layered:

1. *Deadlock.*  langgraph 1.2 defaults =durability="async"=, which submits
   every checkpoint =put= to a =BackgroundExecutor= chained via futures
   (=pregel/_loop.py:_checkpointer_put_after_previous=).  Under deep
   streamed sub-agent nesting the thread pool exhausts and the puts
   deadlock waiting on each other — backend-independent (NOT the sqlite
   connection).  The dump showed ThreadPoolExecutor workers all parked in
   =_checkpointer_put_after_previous=.  Fixed by passing =durability="sync"=
   to =agent.stream()= in =assist.Thread.stream_message=.

2. *Bug A — Overwrite parsing.*  Once turns completed, a node overwriting
   the =messages= channel delivered a =langgraph.types.Overwrite= wrapper
   (not a list); =stream.py:_tool_messages= crashed with =TypeError:
   'Overwrite' object is not iterable=.  Fixed via the shared
   =assist.stream_chunks.unwrap_messages=.

3. *Bug B — cross-thread generator.*  =_stream_turn= advanced the
   thread-affine stream generator (it holds =THREAD_QUEUE.acquire='s
   Condition + a ContextVar) across the default thread pool and closed it
   on the event-loop thread → the lock was released by a non-owning thread
   → =RuntimeError: cannot release un-acquired lock= → worker crash.  Fixed
   by pinning build/next/close to a dedicated =max_workers=1= pump so the
   whole generator lifecycle stays on one thread + context (mirrors the
   assist CLI, immune because it drives the generator on one thread).

*Resolution.*  assist branch =fix-streamed-subagent-deadlock= (durability +
=unwrap_messages= + CLI fix); emacsos branch =fix-streamed-chat-langgraph-1.2=
(=stream.py= routing + the single-thread pump).  Verified e2e 2026-05-25:
research turn completes start→end, sub-agent turn runs without crashing,
=_STREAM_LOCK= releases between turns, web blocking path unaffected.

*Original (WRONG) theory, kept for the record:* blamed =5f0dd90= "Persist
conversation history" for adding a process-wide =SqliteSaver= that
sub-agents supposedly contended on via a shared sqlite cursor.  The py-spy
dump showed the stall is langgraph's checkpoint-put pool, not the
connection — isolating the sub-agent checkpointer (the
=isolate-subagent-checkpointer= branch) did NOT fix it, confirming the
theory was wrong.  Client disconnect was likewise a red herring.

** TODO call_emacs return tuple → typed kind

`channel.eval_elisp`'s `is_unreachable` classifier reverse-engineers
`phone.call_emacs`'s error-string shapes (`emacsclient:` prefix,
`exit N`, `[Errno N]`, `timed out`, `auth file:`, `binary not
found`).  Brittle: changing the wording in `phone.py` silently
breaks the no-retry hint.  Lift the classification into
`call_emacs`'s return shape (eg. `CallResult(kind: Literal["ok",
"unreachable", "elisp_error"], output: str)`) and switch in the
tool.  Deletes ~12 lines of substring matching + the dedicated
unreachable-shape tests.

** DONE Keep chat and elisp source out of request logs

*Shipped 2026-09-03.*  The =/chat= request and =eval_elisp= INFO records log
only source lengths, not the user's message or evaluated Elisp.  This avoids
depending on incomplete substring redaction for arbitrary private content.

** DONE Remote connectivity: WireGuard for the away phone

*Shipped 2026-05-31* —
[[file:docs/2026-05-31-wireguard-away-phone.org][docs/2026-05-31-wireguard-away-phone.org]].
Two-peer topology (home dev box ↔ phone, no relay box — the home server is
already publicly reachable).  Phone wg conf uses the dev box's stable EUI-64
IPv6 as Endpoint so the same line works over wifi and over T-Mobile cellular
(which is IPv6-only).  =MTU = 1280= on the phone wg interface to survive
cellular PMTU + wg overhead for TCP.  Server-side firewall: IPv4 port-forward +
IPv6 inbound pinhole for UDP 51821.  Reproducible setup via
=deploy/wireguard/{server-add-peer,phone-bringup}.sh= + =make wg-add-peer= /
=make wg-phone-bringup=; private keys are generated on the phone and never
leave it.  Notes the sharp edges captured in the doc (wg-quick bounce required
when endpoint family changes; LAN ssh to phone is broken by the AllowedIP
trade-off — use =ssh phone-wg= → =10.0.0.X=; rfkill-persists-across-reboots
trap for cellular cutover, addressed in the playground scripts).

The original write-up below is preserved for historical context.

Today the whole channel assumes the phone and server share a LAN.
The phone POSTs =/chat= to the server's local IP, and the server
reaches *back* to the phone's emacs daemon via =emacsclient=
(=phone.call_emacs=), targeting whatever address the HTTP request
came from (=request.client.host=).  That return path only works
because both are on the same network.

The real deployment: *server stays home, phone goes out into the
world.*  When the phone is remote it connects home over WireGuard.
Two problems to solve:

1. *Server must be a WireGuard peer the phone can be reached at.*
   The back-channel (=eval_elisp= → =call_emacs= → phone) needs the
   server to *initiate* a connection to the phone.  That means a wg
   topology where the server can route to the phone's tunnel IP
   (phone as a peer with appropriate =AllowedIPs=; server side
   configured to reach it).  Without this, the agent can receive a
   request from the away phone but can't act on it.

2. *Home-vs-away detection → pick the route.*  When home, reach the
   phone at its LAN IP (lower latency, no tunnel); when away, reach
   it at its wg IP.  The existing =request.client.host= substitution
   in =phone.py= may already do most of this for free: if the phone
   always POSTs from the interface it's reachable on (LAN IP at
   home, wg IP away), then the source address of the request IS the
   correct return address.  Worth validating that assumption against
   the actual wg routing before building anything fancier — we may
   not need explicit home/away state at all, just correct wg config
   + the address-from-request trick we already have.

Open questions for the future design doc:
- Does the phone's outbound =/chat= reliably egress on the same
  interface the server should return on?  (Determines whether
  =request.client.host= is sufficient or we need explicit detection.)
- wg keepalive / NAT-traversal so the server-initiated back-channel
  survives the phone being behind cellular CGNAT.
- Latency budget: =call_emacs= has a 15s timeout (=EVAL_TIMEOUT_
  SECONDS=); a wg round trip over cellular may need a higher cap for
  the away case.
- Auth-file host field: =parse_auth_file= already discards the
  posted host and substitutes =request.client.host=, so the phone
  doesn't need to know its own reachable address — good, that part
  already generalises to wg.

* Experiment-specific follow-ups

For now these live in the "Out of scope, next experiments"
section of each landed experiment's design doc:

- [[file:docs/2026-05-17-assist-import.org][2026-05-17-assist-import.org]] — skill discovery, honesty
  validation, agent-callable phone tools, streaming, persistent
  thread state, sandbox, dotfiles bootstrap, known-good marker


* Usability

These add EmacsOS's device-side system-integration layer.  The EmacsOS way is
Elisp wrapping system CLIs asynchronously, surfaced as dedicated buffers and
ordinary interactive commands.  Numbers and symbols are shipped.  The current
network page reads WiFi and modem state through NetworkManager and ModemManager;
protected credential entry and the bounded Controls handoff are shipped.

** DONE Wifi connection flow
The =*network*= buffer lists SSIDs with signal and security, shows the active
connection, and connects saved, open, or newly secured networks asynchronously.
Credentials stay out of argv and cross the privilege boundary through bounded
stdin.  The PinePhone Controls row supplies explicit radio setters and opens a
fixed four-network chooser whose Done action returns to Controls.

** DONE Numbers + symbols on the keyboard
The CAPS button now cycles through =abc=, =ABC=, =123=, and =#+== layers.  Digits,
common punctuation, URLs, WiFi passwords, and phone numbers can all be entered
without leaving the keyboard.

*Design (chosen):* the CAPS button becomes a 4-state CYCLE — abc (lower) → ABC
(caps lock) → 123 (numbers, 9+0 share the last key) → #+= (symbols, packed since
no digits compete) → loop.  One button does shift + layer.  =MOD= (C/M) still
applies in every mode (C-7/M-digit, C-/ undo, etc.) — it filters whatever the
active layer is, not just letters.

*** TODO Quick lowercase after a capital (caps-cycle follow-up)
The unified CAPS cycle makes returning ABC→abc a 3-tap trip, which hurts
Title-case typing.  Solve separately — idea: *long-press a letter key* to toggle
the case of the just-typed letter (or a one-shot shift).  Decide on the phone.

** TODO Speech to text
T9 is terrible.

*STT VALIDATED (2026-06-18)* — =faster-whisper= =base.en= on *CPU* transcribed a
real call clip accurately (the RTX 3090 is occupied by llama.cpp, ~2 GB free, so
CPU; fast enough for short clips).  Pipeline + gotchas (16 kHz, normalize, VAD)
in [[file:docs/2026-06-18-call-audio.org][docs/2026-06-18-call-audio.org]].  The
Pi Zero *2* W is quad-core 64-bit (the "too weak" note below was for the Zero W).

Push-to-talk dictation as a faster input path.  The Pi Zero W is too weak for
real-time on-device whisper (single-core ARM11, 512 MB), so do it server-side — the
home assist server is already there: a command records via ALSA (=arecord=, needs
the mic from the audio build) and POSTs the wav to a new STT endpoint on the server
(=whisper.cpp= / faster-whisper on the GPU box), which returns text to insert at
point.  Reuses the existing phone↔server channel shape.  Depends on the audio build
(mic) and the server endpoint; big enough to want its own design doc.  Done when
hold-speak-release drops accurate text into the current buffer.

** DONE Phone calling
Shipped in PRs #25/#26/#29/#30 (2026-06-18..20): deterministic dial/hang-up,
contact resolution through the skill, visible call state, and keyboard controls.
Pierre confirmed the phone can place real calls.  Agent-assistant audio is tracked
separately under the P3 full-duplex validation item.

** DONE Phone answering
Shipped in PR #28 (2026-06-19): ModemManager D-Bus ring detection, incoming-call
screen, raw-AT answer, and call-state dismissal.  Pierre confirmed the phone can
receive and answer real calls.  PR #34 intentionally adds a separate headless
voice-assistant watcher/bridge; SMS forwarding remains separate and unchanged.

** PARTIAL Text messaging

Confirmed outbound SMS is shipped: a user or local chat agent stages an exact
recipient and body, the phone shows the proposal, and two physical taps send it
through a bounded ModemManager D-Bus helper.  The helper reports sent, not-sent,
or unknown without leaking message content.  This change adds bounded native
per-number conversations, trusted watcher-backed inbound delivery, and bounded
history refresh while retaining the local two-tap send authorization.

Remaining: agent-assisted handling of SMS threads.  Automatic retry remains
deliberately absent, and raw AT/PDU work is not required for this path.

** TODO Modeline status bar (time / date / battery / signal)
Display things like the date, time and battery percentage. Consider removing most of
everything else from the modeline.

*PARTIAL* — the original *network (wifi/cell) segment* shipped with the cellular branch
([[file:docs/2026-05-26-cellular-data-connectivity.org][docs/2026-05-26-cellular-data-connectivity.org]]):
=mode-line-format= is now a minimal global default (=setq-default= at the top of
=os.el=) carrying the compact 0.8-height =" EmacsOS"= label + a tappable
=:eval= segment from
=emacsos-net-mode-line-string= (=network.el=), with the stock clutter (buffer
position, minor modes, encoding) stripped.  On the PinePhone, that network
entry and the adjacent keyboard entry are now replaced by one compact, tappable
=Controls= entry.  It opens the phone-sized surface for modem data, brightness,
flashlight, keyboard recovery, and the bounded Wi-Fi chooser; call and SMS
badges remain independent.  *REMAINING:* time + date
(=display-time-mode=), battery % (=display-battery-mode= reads
=/sys/class/power_supply/=), and any residual clutter — append into the same list.

*Battery hardware (this build, measured 2026-05-30):* a single Li-ion cell feeds the
Pi via 2× [[https://www.adafruit.com/product/2465][Adafruit PowerBoost 1000C (#2465)]]
— DC/DC boost + LiPo charger.  The PowerBoost reports state via *analog LEDs only*
(low-battery red / charging yellow / full green); there is *no I2C/SPI* digital
readout, so nothing publishes a =/sys/class/power_supply/= node and
=display-battery-mode= has nothing to read (confirmed empty 2026-05-30).
*For a numeric % in the modeline:* add a *fuel-gauge IC* (e.g. MAX17048 / LC709203F)
on I2C across the same battery, whose driver exposes a =power_supply= node.
*Cheaper interim:* wire one of the PowerBoost's status pins (LBO / CHG / DONE) to a
GPIO and surface a charging / low-battery *flag* (no %) in the modeline.

** TODO 

* Actual phone build
There are things outside of software that need to be done to make this phone truly useable in the wild.

These hardware items gate the cellular software above (calling / answering /
texting) and STT.  Worth one design pass since the modem, the audio I/O, and the
call-audio routing between them are interdependent — pick them together, not
separately.

** TODO Pi rail browns out / reboots when the PowerBoost charge input is unplugged
Observed 2026-06-20: with battery → PowerBoost 1000C → (USB-A out) → Pi =PWR IN=,
*unplugging the wall charger from the PowerBoost's micro-USB charge input reboots
the Pi.* The PowerBoost's USB-power↔battery *load-share transition* dips its 5 V
output enough to brown out the Zero 2 W. Not urgent (don't hot-swap the charge
input under load), but fix for field use. Candidate fixes: a *bulk cap on the Pi
5 V* to ride the transition; verify the PowerBoost's load-share jumper/config; or
move the Pi to a power-path that doesn't glitch on input removal. Tracks with the
[[file:docs/hardware-build.org][power-rig work]] (Rail 1).

** TODO Smaller cellular antenna (the bundled whip is huge/clunky)
The SIM7600's bundled whip antenna is bulky. Replace with a small *flexible-PCB
(FPC) antenna on a u.FL/IPEX connector* — flat, adhesive-backed, far smaller.
OURS used an [[https://www.communica.co.za/products/hkd-gsm-antenna-on-ipex-13-con][HKD GSM antenna on IPEX-13]] (~R19) — get a *multiband 4G/LTE* FPC
version (not GSM-only) so it covers the carrier's LTE bands. ~$3-8. Confirm the
connector matches the modem's MAIN antenna port (u.FL). Not urgent.

** TODO Finish stable-power live validation of full-duplex call-audio bridge
The synchronized bridge daemon now exists in PR #34.  Its single balanced pump
reads ttyUSB4 downlink and writes matched uplink frames clocked by the modem; it
uses digital PCM, so a TRRS microphone is not part of this path.  The remaining
blocker is repeatable Pi undervoltage under incoming-call modem load.  After the
power path is fixed, complete the ordered live checks in
[[file:docs/2026-07-28-voice-call-p3-bridge.org][the P3 bridge state doc]].

** TODO Cellular modem ("3G antenna")
I have an antenna, just need to connect it and ensure I can connect to a cell network (reliably, with some options around it).

*Data milestone in design* — [[file:docs/2026-05-26-cellular-data-connectivity.org][docs/2026-05-26-cellular-data-connectivity.org]]:
SIM7600G-H via in-kernel =qmi_wwan= + a NetworkManager =gsm= connection
(=ModemManager= owns the modem; NM owns route + wifi↔cell failover),
provisioned by =make cellular-bringup APN=...=.  "Done when" stops at
public-internet data; reaching the home server from away layers the
WireGuard item on top (the server→phone back-channel needs it).

Connect the modem to the Zero W (USB-OTG or a UART/HAT), insert a SIM, and bring it
up — prefer =ModemManager= + =mmcli= (registration, signal, APN/bearer, SMS and voice
in one stack) over hand-rolled =AT= on =/dev/ttyUSB*=.  "Options around it" = APN
config, a signal monitor, and an auto-reconnect watchdog.  Two things to settle first
because either can sink the plan: (1) modem model + band vs. the local carrier — *3G
is being sunset in many markets*, so verify the modem does LTE if 3G is gone there;
(2) whether the modem supports *voice* at all (many data modems don't, or need a
separate analog audio path) — this decides whether Phone calling is even possible.
Prerequisite for calling, answering, and texting.  Done when the modem registers,
reports signal, and does data + SMS + (if voice-capable) a test call.

** TODO Audio I/O ("headphone jack")
I need to purchase and connect a headphone jack onto the pi zero w.

*UPDATE (2026-06-18)* — see [[file:docs/2026-06-18-call-audio.org][docs/2026-06-18-call-audio.org]].
The modem HAT is the Waveshare *4G HAT (B)*: it already has an onboard NAU8810
codec + a 3.5mm TRRS headset jack (call audio via =AT+CSDVC=, the analog
fallback) *and* exposes call audio as a raw PCM stream on =/dev/ttyUSB4=
(=AT+CPCMREG=1=) — the software path, which records/transcribes today.  For
*media* audio (TTS/STT, not the call): a USB audio dongle (Sabrent AU-MMSA) into
a free USB-A port on the HAT's built-in hub — no extra OTG hub needed.  Two
inherently-separate endpoints; unifying onto one headset is the Option-1 bridge.

The Zero W has *no analog audio* — no 3.5mm jack and no analog out on the header
(unlike full-size Pis) — so this is really "add audio I/O", output *and* a mic (calls
and STT both need capture).  Options: an I2S DAC/codec HAT (e.g. MAX98357A out + an
I2S or USB mic, or a combined codec like the WM8960), a USB audio dongle (needs an OTG
hub — there's one micro-USB data port), or PWM-on-GPIO out (poor, and output-only —
not enough for calls).  The hard part is *call-audio routing*: the modem's voice path
has to reach speaker + mic — either the modem exposes digital audio (USB/PCM) ALSA can
route, or its analog codec pins must be wired to the audio device.  Resolve this
jointly with the modem choice.  Done when ALSA can play and record on-device and
in-call audio works end to end.

** TODO 
