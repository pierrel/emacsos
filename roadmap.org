#+title: EmacsOS Roadmap
#+date: 2026-05-17

Each experiment is its own design doc + branch (see =docs/= for
landed ones).  This file collects forward-looking work that
doesn't fit any specific experiment yet — usability iteration,
cross-cutting concerns, follow-ups discovered live on the phone.

* Ergonomics iteration

Live testing of PR #6 (assist-import) surfaced several rough edges
in the phone UX that aren't load-bearing for any specific
experiment but compound across all of them.  Worth a dedicated
pass.

** DONE Ctrl/Meta modifier keys

Shipped on the =modifier-keys= branch: a leftmost MOD button on the
action row cycles =nil → C → M → C-M → nil=; under a non-nil
modifier, the T9 grid filters to letters that have a binding under
the current modifier in the target buffer's full active-keymap stack
(via =key-binding=), and a tap *fires* the bound command instead of
inserting a letter.  Single-letter fast-path (one bound letter →
fire on tap, no wait); multi-letter subset → arm-then-commit with
multi-tap cycling.  Sticky modifier (only an explicit MOD tap
changes it).  QUIT abandons an armed binding; every other utility
tap commits it.  Shift was rolled into the existing CAPS (locked
decision; under MOD, caps is ignored and canonical lowercase keys
go to the keymap walker).  Full design + decisions in
[[file:docs/2026-05-27-modifier-keys.org][2026-05-27-modifier-keys.org]].

** DONE Dynamic keyboard surface based on top buffer

Shipped as the *single keyboard + commands combo*
([[file:docs/2026-05-21-keyboard-ergonomics.org][docs/2026-05-21-keyboard-ergonomics.org]], §"Rework", PR #11).  An
earlier swap-page design (the bottom window showed ONE of
KBD/CMD/MODE/CHAT at a time, auto/pinned) was *rejected on review* —
it hid the keyboard.  As built: the T9 keyboard is ALWAYS visible; a
*command list* under it shows up to 10 of the top buffer's mode commands
(=*chat*= → SEND + CLEAR/ABORT; a mode in =emacos-mode-commands= → its
set; everything else → =emacos-global-commands=); a persistent utility
row =[QUIT] [M-x] [Chat]= keeps escape, manual command entry, and the
chat app one tap away.  The list follows the top buffer via
=window-buffer-change-functions=.  (Layout v2: uniform button height,
scrollable; DEL (1/3) + SPC (2/3); CAPS/TAB/RET with double-wide RET;
QUIT = smart escape that clears popup/minibuffer clutter off the top.)

Remaining follow-ups (the combo makes these safe to grow incrementally
— the global-commands fallback means an un-enriched mode still works):

*** TODO Enrich =emacos-mode-commands= for more modes

Today: org, emacs-lisp, dired.  Add magit, prog-mode/text-mode
defaults, etc.  Order each list by priority — the command list shows up
to =emacos--max-commands= (10), one per row (overflow → M-x).  React on
the phone to decide which commands earn a slot.

*** TODO Transient-buffer derivation ignore-list

The command list re-derives on =window-buffer-change=; transient popups
(=*Completions*=, =*Help*=) briefly on top can flip it.  The
unchanged-set no-op absorbs most, but a popup whose mode has a command
set would flicker.  Add an ignore-list (or special-mode skip) if it's
annoying live.

*** TODO Chat button doubles as SEND inside *chat*; drop the SEND button

The utility row's =Chat= button switches the top window to =*chat*=.
When you're ALREADY in =*chat*=, "go to chat" is a no-op — so make the
=Chat= button SEND the current input instead, and remove the dedicated
SEND entry from the chat command list.  Net: one fewer button, and the
most-used chat action moves to the always-present utility row.  =Chat= =
"open chat" when elsewhere, "send" when already in =*chat*=.

** DONE Tab button on the keyboard

Shipped in PR #11: TAB is the 4th button on the SPC/RET/DEL action row
(its own =action-w= so the letter keys don't shrink).  Context-aware —
=minibuffer-complete= in the minibuffer, else =indent-for-tab-command=.

** DONE Keyboard layout + sizing iterations

The Optimal-T9 layout from the Qin et al. paper is the v1
baseline.  Live use will surface things the paper doesn't capture
on this specific screen (gap proportions, scale factor between
letter and util buttons, fingertip-vs-stylus accuracy bias,
number row treatment, punctuation strategy).  Iterate based on
what's actually painful in real use rather than redesigning
preemptively.

** TODO Font choice + sizing

The default emacs face on the phone is whatever the system X
fontconfig defaults pick — not optimized for a 320x240 surface
held at arm's length.  Pick a font (and sizes for transcript /
input / keyboard / status) that reads well at this density:
legibility at small px counts, distinguishable lowercase l vs.
uppercase I vs. digit 1, comfortable line-height in =*chat*=.
Candidates: a programming font (Iosevka, JetBrains Mono) tuned
for clarity at small sizes, or a screen-optimized humanist like
Inter.  Try a couple, decide on the phone, codify in =os.el=
(or a per-face block in the init snippet) so phone-install
sets it consistently.

* Tool-call transparency

The emacs-modifying channel ships with `status: calling <tool_name>`
events for each new tool invocation.  Intended UX was richer
(`status: <tool_name>: <truncated args>`) but the implementation
needs `tool_call_chunks` accumulation — args generally arrive across
many continuation chunks, not on the first chunk where the tool
name lands.  Follow-up work: stitch `tool_call_chunks` per
tool_call_id, emit `<name>: <args>` when complete (or stream args
character-by-character — see below).

Other dimensions worth iterating once the basic args preview lands:
- *Tool-result visibility.*  Right now elisp results stay inside the
  langgraph trace; the user sees only the agent's prose summary
  afterwards.  For a device the agent is MODIFYING this may be too
  opaque — a `status: eval_elisp result: <preview>` event would let
  the user spot a wrong-direction call early.  Trade-off: noisier
  on multi-call sessions; might warrant a "verbose" toggle.
- *Streaming partial args.*  Show args character-by-character as
  they stream in (like the bot text already does).  Costs an event
  per chunk per arg; consider only for slow tool-args generation.

* Other / cross-cutting

** DONE Drop the deprecated =loop_exploration_tools= kwarg (SMALL — do next)

Done on branch =port-to-agentspec= (2026-06-11), folded into the AgentSpec
port: the kwarg and =LOOP_EXPLORATION_TOOLS= are gone from =_build_thread= /
=channel.py=.

assist rolled back its LoopDetectionMiddleware to only the two exact-repeat
patterns (same-tool-same-error / same-tool-same-args) and removed the
distinct-args "exploration breadth" pattern that =loop_exploration_tools= fed.
The kwarg is now a deprecated *no-op* on the assist side: =create_agent= /
=Thread= still ACCEPT it (so we don't break) but ignore it.

We still pass it:
- =server/emacsos_server/app.py:276= → =create_agent(loop_exploration_tools=LOOP_EXPLORATION_TOOLS, ...)=
- =LOOP_EXPLORATION_TOOLS = frozenset({"eval_elisp"})= at =server/emacsos_server/channel.py:298=
- imported at =app.py:34=

Action: drop the kwarg at the call site and remove =LOOP_EXPLORATION_TOOLS=
(and its import).  =eval_elisp= is unaffected — loop detection now applies the
exact-repeat guards uniformly to every tool, so there's nothing to tune per
tool.  Once emacsos stops passing it, assist can delete the no-op param
entirely (its docstrings already point here).

** DONE Conversation persistence across /chat requests

Shipped: =_build_thread= now persists the conversation across /chat turns
via a fixed thread id + a =SqliteSaver= checkpointer on
=<state_dir>/threads.db= — the Thread OBJECT is rebuilt each request, but
its state lives in the checkpointer (replayed by thread id), instead of
the old fresh-state-per-request — so the agent remembers prior turns
("now make it bigger", "make that permanent").  Reset is an explicit
"New chat" (=POST /clear= → =delete_thread= + a wipe of the conversation
working dir, which clears the agent's =AGENTS.md= memory too).  Context-window growth is
delegated to deepagents' =SummarizationMiddleware= (no custom windowing);
the raw log accumulates in threads.db but the model context stays
bounded, and New chat is the release valve.  See
[[file:docs/2026-05-22-conversation-history.org][docs/2026-05-22-conversation-history.org]].
Keyed by a single fixed id (one personal phone) — per-phone keying is
deferred with the WireGuard/away-phone work.

** TODO Flexible config change management (revert any past change via chat)

The next experiment *immediately after* conversation history — we go
straight here, deliberately skipping any intermediate "N-level only"
chat-revert step.

Today config undo is strictly one-level: the phone ROLLBACK button calls
=config_repo.rollback()= → =git revert --no-edit HEAD=, undoing only the
most recent =apply_config=.  *That one-level button stays as-is* — it's
the quick "undo the thing I just did" fast path, and it's all the button
needs to be.

The full system is conversational and *targeted*: "revert the change
about X, where X is any past apply" — e.g. "undo the cursor color change
but keep the font change", or "revert the last two things".  Driven from
*chat* (it needs the conversation context that conversation history
unlocks), the agent:
- surfaces the apply history to itself — each =apply_config= already
  commits with its SUMMARY as the git message, so =git log= IS the
  change list; no new persistence needed;
- maps a natural-language reference ("the cursor change") to a commit;
- reverts just that commit (=git revert --no-edit <sha>=), or a chosen
  set/range, history-preserving so roll-forward stays possible;
- handles conflicts honestly (=git revert --abort= + a "can't cleanly
  revert that — a later change touched the same thing" result) when a
  targeted/range revert doesn't invert cleanly.

So this single item subsumes what was once "chat-revert" + "N-level" +
"targeted": the one-level phone button is the fast path; this is the
rich, conversational path, and it's where we head next.

*** TODO Agent can't read current config after =/clear= (prerequisite)

Surfaced 2026-05-23 by a live "set the cursor blue" turn right after a
=/clear= ("New chat").  =apply_config= replaces the whole config file, so
the config-apply skill tells the agent to *read the current config first*
and restate it.  But with conversation memory wiped by =/clear=, the agent
has no way to *find* the config: it tried =(user-init-file)=,
=(load-file)=, =(getenv "EMACS_CONFIG")= — none reach
=~/.emacs.d/emacsos/agent.el= (a server-side path it can't know) — and
LoopDetection then caught the distinct-args thrash and terminated the
turn.  Nothing was applied (the failure was *safe*), but the user's most
likely action after a reset — "now change X" — currently can't persist.

Root cause: the agent relies on conversation *memory* as the source of
truth for current config.  That memory is double-edged — with it the
agent can restate config (but resurrects stale settings, e.g. the
line-numbers bug that =/clear= was meant to cure); without it
(post-=/clear=) it can't apply at all.

Fix direction: stop relying on memory — give the agent a reliable read
path (a =get_config= / =read_config= tool, or inject the current
=agent.el= body into the skill/system context).  Then =/clear= is safe
(memory gone, config still readable) AND there's no resurrection (the
committed file is the source of truth, not memory).  A prerequisite for
the flexible config management above — that work also needs the agent to
navigate config state, so fold it in or land it first.

** DONE Phone tempdir leak

Fixed as a consequence of conversation persistence (above): the
per-/chat `tempfile.mkdtemp` is gone — the persistent conversation now
uses ONE stable working dir under `<state_dir>/threads/`, reused across
turns, so there's no per-turn directory to accumulate.

** DONE Sub-agent turns wedge the server (langgraph 1.2 streamed-path bugs)

Any /chat turn that spawned a SUB-AGENT (=task= → =research-agent=,
=context-agent=) wedged the moment the sub-agent ran: no next model call,
no error, just silence; then the orphaned worker wedged EVERY later turn
until the uvicorn worker was restarted.

*Real root cause* — a py-spy dump (2026-05-25) DISPROVED the original
shared-=SqliteSaver=-contention theory (kept at the bottom for the record).
Three distinct bugs, all from the langgraph 1.2.0 upgrade, layered:

1. *Deadlock.*  langgraph 1.2 defaults =durability="async"=, which submits
   every checkpoint =put= to a =BackgroundExecutor= chained via futures
   (=pregel/_loop.py:_checkpointer_put_after_previous=).  Under deep
   streamed sub-agent nesting the thread pool exhausts and the puts
   deadlock waiting on each other — backend-independent (NOT the sqlite
   connection).  The dump showed ThreadPoolExecutor workers all parked in
   =_checkpointer_put_after_previous=.  Fixed by passing =durability="sync"=
   to =agent.stream()= in =assist.Thread.stream_message=.

2. *Bug A — Overwrite parsing.*  Once turns completed, a node overwriting
   the =messages= channel delivered a =langgraph.types.Overwrite= wrapper
   (not a list); =stream.py:_tool_messages= crashed with =TypeError:
   'Overwrite' object is not iterable=.  Fixed via the shared
   =assist.stream_chunks.unwrap_messages=.

3. *Bug B — cross-thread generator.*  =_stream_turn= advanced the
   thread-affine stream generator (it holds =THREAD_QUEUE.acquire='s
   Condition + a ContextVar) across the default thread pool and closed it
   on the event-loop thread → the lock was released by a non-owning thread
   → =RuntimeError: cannot release un-acquired lock= → worker crash.  Fixed
   by pinning build/next/close to a dedicated =max_workers=1= pump so the
   whole generator lifecycle stays on one thread + context (mirrors the
   assist CLI, immune because it drives the generator on one thread).

*Resolution.*  assist branch =fix-streamed-subagent-deadlock= (durability +
=unwrap_messages= + CLI fix); emacsos branch =fix-streamed-chat-langgraph-1.2=
(=stream.py= routing + the single-thread pump).  Verified e2e 2026-05-25:
research turn completes start→end, sub-agent turn runs without crashing,
=_STREAM_LOCK= releases between turns, web blocking path unaffected.

*Original (WRONG) theory, kept for the record:* blamed =5f0dd90= "Persist
conversation history" for adding a process-wide =SqliteSaver= that
sub-agents supposedly contended on via a shared sqlite cursor.  The py-spy
dump showed the stall is langgraph's checkpoint-put pool, not the
connection — isolating the sub-agent checkpointer (the
=isolate-subagent-checkpointer= branch) did NOT fix it, confirming the
theory was wrong.  Client disconnect was likewise a red herring.

** TODO call_emacs return tuple → typed kind

`channel.eval_elisp`'s `is_unreachable` classifier reverse-engineers
`phone.call_emacs`'s error-string shapes (`emacsclient:` prefix,
`exit N`, `[Errno N]`, `timed out`, `auth file:`, `binary not
found`).  Brittle: changing the wording in `phone.py` silently
breaks the no-retry hint.  Lift the classification into
`call_emacs`'s return shape (eg. `CallResult(kind: Literal["ok",
"unreachable", "elisp_error"], output: str)`) and switch in the
tool.  Deletes ~12 lines of substring matching + the dedicated
unreachable-shape tests.

** TODO Redact secrets in `/chat` request log

`app.py`'s `log.info("POST /chat msg=%r", req.message)` logs the
user's chat input verbatim.  `channel._redact` exists and could be
applied here.  Surfaced by the code-review team; not done in the
channel PR because that PR shouldn't widen scope to `app.py`'s
logging.

** DONE Remote connectivity: WireGuard for the away phone

*Shipped 2026-05-31* —
[[file:docs/2026-05-31-wireguard-away-phone.org][docs/2026-05-31-wireguard-away-phone.org]].
Two-peer topology (home dev box ↔ phone, no relay box — the home server is
already publicly reachable).  Phone wg conf uses the dev box's stable EUI-64
IPv6 as Endpoint so the same line works over wifi and over T-Mobile cellular
(which is IPv6-only).  =MTU = 1280= on the phone wg interface to survive
cellular PMTU + wg overhead for TCP.  Server-side firewall: IPv4 port-forward +
IPv6 inbound pinhole for UDP 51821.  Reproducible setup via
=deploy/wireguard/{server-add-peer,phone-bringup}.sh= + =make wg-add-peer= /
=make wg-phone-bringup=; private keys are generated on the phone and never
leave it.  Notes the sharp edges captured in the doc (wg-quick bounce required
when endpoint family changes; LAN ssh to phone is broken by the AllowedIP
trade-off — use =ssh phone-wg= → =10.0.0.X=; rfkill-persists-across-reboots
trap for cellular cutover, addressed in the playground scripts).

The original write-up below is preserved for historical context.

Today the whole channel assumes the phone and server share a LAN.
The phone POSTs =/chat= to the server's local IP, and the server
reaches *back* to the phone's emacs daemon via =emacsclient=
(=phone.call_emacs=), targeting whatever address the HTTP request
came from (=request.client.host=).  That return path only works
because both are on the same network.

The real deployment: *server stays home, phone goes out into the
world.*  When the phone is remote it connects home over WireGuard.
Two problems to solve:

1. *Server must be a WireGuard peer the phone can be reached at.*
   The back-channel (=eval_elisp= → =call_emacs= → phone) needs the
   server to *initiate* a connection to the phone.  That means a wg
   topology where the server can route to the phone's tunnel IP
   (phone as a peer with appropriate =AllowedIPs=; server side
   configured to reach it).  Without this, the agent can receive a
   request from the away phone but can't act on it.

2. *Home-vs-away detection → pick the route.*  When home, reach the
   phone at its LAN IP (lower latency, no tunnel); when away, reach
   it at its wg IP.  The existing =request.client.host= substitution
   in =phone.py= may already do most of this for free: if the phone
   always POSTs from the interface it's reachable on (LAN IP at
   home, wg IP away), then the source address of the request IS the
   correct return address.  Worth validating that assumption against
   the actual wg routing before building anything fancier — we may
   not need explicit home/away state at all, just correct wg config
   + the address-from-request trick we already have.

Open questions for the future design doc:
- Does the phone's outbound =/chat= reliably egress on the same
  interface the server should return on?  (Determines whether
  =request.client.host= is sufficient or we need explicit detection.)
- wg keepalive / NAT-traversal so the server-initiated back-channel
  survives the phone being behind cellular CGNAT.
- Latency budget: =call_emacs= has a 15s timeout (=EVAL_TIMEOUT_
  SECONDS=); a wg round trip over cellular may need a higher cap for
  the away case.
- Auth-file host field: =parse_auth_file= already discards the
  posted host and substitutes =request.client.host=, so the phone
  doesn't need to know its own reachable address — good, that part
  already generalises to wg.

* Experiment-specific follow-ups

For now these live in the "Out of scope, next experiments"
section of each landed experiment's design doc:

- [[file:docs/2026-05-17-assist-import.org][2026-05-17-assist-import.org]] — skill discovery, honesty
  validation, agent-callable phone tools, streaming, persistent
  thread state, sandbox, dotfiles bootstrap, known-good marker


* Usability

These add EmacsOS's first device-side system-integration layer: today =os.el= is
pure UI (keyboard, command list, =*chat*=) and shells out to nothing.  The EmacsOS
way is elisp wrapping the system CLIs (=call-process= / =start-process=), surfaced
as dedicated buffers/modes that plug into the always-visible keyboard +
=emacos-mode-commands= list.  Dependency order isn't the list order below:
*Numbers* unblocks Wifi (passwords) and Phone calling (dialing), and the three
cellular items block on the "Actual phone build" hardware.  Text messaging is the
cheapest cellular win (no audio), so it's the natural first proof the modem works.

** TODO Wifi connection flow
I need a mode for seeing and connecting to wifi.

A =emacos-wifi-mode= buffer (=tabulated-list-mode=) that scans and lists SSIDs with
signal + security, shows the current connection, and offers Connect / Disconnect /
Forget as =emacos-mode-commands= entries; passphrase entry runs through the existing
keyboard into a masked minibuffer prompt.  Back it with the image's network stack —
confirm whether that's =nmcli= (NetworkManager) or =wpa_cli= (=wpa_supplicant=);
prefer =nmcli= (far easier to script, and saved-network + auto-reconnect come free),
installing it if absent.  This is the first =call-process=-to-system-tool binding,
so it sets the pattern for the cellular work.  Depends on *Numbers* (passwords).
Done when you can join a WPA2 network from cold, entirely on-device.

** TODO Numbers + symbols on the keyboard
I need to be able to input numbers.

The T9 grid is letters-only, so there's no way to type a digit — which blocks wifi
passwords, URLs, and phone numbers.  Add a =123/ABC= toggle on the utility row that
swaps the 3x3 letter grid for a digit + common-punctuation layer (phone-keyboard
style), keeping multi-tap so one key still reaches the rarer symbols.  Lives in
=os.el= next to =emacos--tap-key= and the layout table.  Decide on the phone which
symbols earn a slot (=. : / @ - _= are the high-frequency ones for passwords/URLs).
Prerequisite for Wifi and Phone calling.  Done when you can type a WPA password and
a phone number without leaving the keyboard.

*Design (chosen):* the CAPS button becomes a 4-state CYCLE — abc (lower) → ABC
(caps lock) → 123 (numbers, 9+0 share the last key) → #+= (symbols, packed since
no digits compete) → loop.  One button does shift + layer.  =MOD= (C/M) still
applies in every mode (C-7/M-digit, C-/ undo, etc.) — it filters whatever the
active layer is, not just letters.

*** TODO Quick lowercase after a capital (caps-cycle follow-up)
The unified CAPS cycle makes returning ABC→abc a 3-tap trip, which hurts
Title-case typing.  Solve separately — idea: *long-press a letter key* to toggle
the case of the just-typed letter (or a one-shot shift).  Decide on the phone.

** TODO Speech to text
T9 is terrible.

Push-to-talk dictation as a faster input path.  The Pi Zero W is too weak for
real-time on-device whisper (single-core ARM11, 512 MB), so do it server-side — the
home assist server is already there: a command records via ALSA (=arecord=, needs
the mic from the audio build) and POSTs the wav to a new STT endpoint on the server
(=whisper.cpp= / faster-whisper on the GPU box), which returns text to insert at
point.  Reuses the existing phone↔server channel shape.  Depends on the audio build
(mic) and the server endpoint; big enough to want its own design doc.  Done when
hold-speak-release drops accurate text into the current buffer.

** TODO Phone calling
Needs "3G antenna" and "Headphone jack".

A command + call buffer: enter a number (needs *Numbers*), dial via the modem
(=mmcli --voice= or =AT D=), and show call state (dialing / ringing / connected /
ended) with a Hang-up command.  The blocker is voice support + in-call audio routing
(see the modem and audio items) — if the modem is data-only, calling is off the
table.  Depends on: Cellular modem (voice-capable), Audio I/O (speaker + mic),
Numbers.  Done when you can dial a real number and hold a two-way call.

** TODO Phone answering
Needs "3G antenna".

Surface incoming calls.  A background watcher on the modem's unsolicited events
(=ModemManager= D-Bus signal, or an =AT= notification reader) bridges a RING into the
emacs daemon via =emacsclient= eval — the same external-event→daemon pattern the chat
back-channel already uses — popping an Answer / Decline buffer and routing audio on
Answer.  Shares that watcher process with Text messaging (incoming SMS), so build the
bridge once.  Depends on: Cellular modem, Audio I/O, the shared modem-watcher.  Done
when the phone rings, you can answer, and audio works.

** TODO Text messaging
Needs "3G antenna".

Send/receive SMS via the modem (=mmcli --messaging-*= over raw =AT+CMGS/+CMGR= PDU
wrangling).  A per-contact conversation buffer, compose via the keyboard, receive
through the shared modem-watcher (same bridge as Phone answering).  Persist threads
as org under the agent's dir — greppable, and the agent can read/act on them.  No
audio dependency, so this is the easiest cellular feature and the first end-to-end
proof the modem stack works.  Depends on: Cellular modem, the shared modem-watcher.

** TODO Modeline status bar (time / date / battery / signal)
Display things like the date, time and battery percentage. Consider removing most of
everything else from the modeline.

*PARTIAL* — the *network (wifi/cell) segment* shipped with the cellular branch
([[file:docs/2026-05-26-cellular-data-connectivity.org][docs/2026-05-26-cellular-data-connectivity.org]]):
=mode-line-format= is now a minimal global default (=setq-default= at the top of
=os.el=) carrying the =" EmacsOS"= label + a tappable =:eval= segment from
=emacos-net-mode-line-string= (=network.el=), with the stock clutter (buffer
position, minor modes, encoding) stripped.  *REMAINING:* time + date
(=display-time-mode=), battery % (=display-battery-mode= reads
=/sys/class/power_supply/=), and any residual clutter — append into the same list.

*Battery hardware (this build, measured 2026-05-30):* a single Li-ion cell feeds the
Pi via 2× [[https://www.adafruit.com/product/2465][Adafruit PowerBoost 1000C (#2465)]]
— DC/DC boost + LiPo charger.  The PowerBoost reports state via *analog LEDs only*
(low-battery red / charging yellow / full green); there is *no I2C/SPI* digital
readout, so nothing publishes a =/sys/class/power_supply/= node and
=display-battery-mode= has nothing to read (confirmed empty 2026-05-30).
*For a numeric % in the modeline:* add a *fuel-gauge IC* (e.g. MAX17048 / LC709203F)
on I2C across the same battery, whose driver exposes a =power_supply= node.
*Cheaper interim:* wire one of the PowerBoost's status pins (LBO / CHG / DONE) to a
GPIO and surface a charging / low-battery *flag* (no %) in the modeline.

** TODO 

* Actual phone build
There are things outside of software that need to be done to make this phone truly useable in the wild.

These hardware items gate the cellular software above (calling / answering /
texting) and STT.  Worth one design pass since the modem, the audio I/O, and the
call-audio routing between them are interdependent — pick them together, not
separately.

** TODO Cellular modem ("3G antenna")
I have an antenna, just need to connect it and ensure I can connect to a cell network (reliably, with some options around it).

*Data milestone in design* — [[file:docs/2026-05-26-cellular-data-connectivity.org][docs/2026-05-26-cellular-data-connectivity.org]]:
SIM7600G-H via in-kernel =qmi_wwan= + a NetworkManager =gsm= connection
(=ModemManager= owns the modem; NM owns route + wifi↔cell failover),
provisioned by =make cellular-bringup APN=...=.  "Done when" stops at
public-internet data; reaching the home server from away layers the
WireGuard item on top (the server→phone back-channel needs it).

Connect the modem to the Zero W (USB-OTG or a UART/HAT), insert a SIM, and bring it
up — prefer =ModemManager= + =mmcli= (registration, signal, APN/bearer, SMS and voice
in one stack) over hand-rolled =AT= on =/dev/ttyUSB*=.  "Options around it" = APN
config, a signal monitor, and an auto-reconnect watchdog.  Two things to settle first
because either can sink the plan: (1) modem model + band vs. the local carrier — *3G
is being sunset in many markets*, so verify the modem does LTE if 3G is gone there;
(2) whether the modem supports *voice* at all (many data modems don't, or need a
separate analog audio path) — this decides whether Phone calling is even possible.
Prerequisite for calling, answering, and texting.  Done when the modem registers,
reports signal, and does data + SMS + (if voice-capable) a test call.

** TODO Audio I/O ("headphone jack")
I need to purchase and connect a headphone jack onto the pi zero w.

The Zero W has *no analog audio* — no 3.5mm jack and no analog out on the header
(unlike full-size Pis) — so this is really "add audio I/O", output *and* a mic (calls
and STT both need capture).  Options: an I2S DAC/codec HAT (e.g. MAX98357A out + an
I2S or USB mic, or a combined codec like the WM8960), a USB audio dongle (needs an OTG
hub — there's one micro-USB data port), or PWM-on-GPIO out (poor, and output-only —
not enough for calls).  The hard part is *call-audio routing*: the modem's voice path
has to reach speaker + mic — either the modem exposes digital audio (USB/PCM) ALSA can
route, or its analog codec pins must be wired to the audio device.  Resolve this
jointly with the modem choice.  Done when ALSA can play and record on-device and
in-call audio works end to end.

** TODO 
