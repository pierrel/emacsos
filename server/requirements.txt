fastapi>=0.100
uvicorn[standard]>=0.23
pydantic>=2
requests>=2.28
httpx>=0.24
pytest>=7

# assist is installed editably from $(ASSIST_REPO_DIR) by the
# Makefile (see setup-server).  Editable is a dev-freshness choice, no
# longer a requirement: assist's wheel bundles templates/ and skills/
# since the embedder-contract refactor (2026-06-11), so a plain wheel
# install works too.
