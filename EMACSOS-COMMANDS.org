#+title: EmacsOS commands and keys

Swipe downward from =x= on the modifier-swipe keyboard to type =Meta+x=, then
type a command name and press =RET=.  Run =M-x emacsos-open-command-reference=
to return to this file.

* PinePhone/OpenRC applications

These commands are installed only by the PinePhone/OpenRC deployment; the
generic =phone-install= target does not define them.

| Action | M-x command |
|--------+-------------|
| Start or switch to Firefox | =emacsos-firefox-start= |
| Quit Firefox started by EmacsOS | =emacsos-firefox-quit= |
| Start or switch to Android | =emacsos-android-start= |
| Stop Android | =emacsos-android-quit= |
| Hide or show the keyboard | =emacsos-pinephone-toggle-keyboard= |
| Restart the supervised phone UI | =emacsos-pinephone-restart-ui-session= |

Use =Alt+Tab= to move between full-screen applications.  Return to ordinary
chat with =M-x emacsos--chat-show-top-buffer=.

* Chat and messages

| Action | M-x command |
|--------+-------------|
| Return to ordinary chat | =emacsos--chat-show-top-buffer= |
| Start a new ordinary chat | =emacsos--chat-new-chat= |
| Stop the current ordinary-chat response | =emacsos--chat-abort= |
| Browse Assist Web threads | =emacsos-assist-web-show-thread-list= |
| Open an Assist Web thread | =emacsos-assist-web-open-thread= |
| Start an Assist Web thread | =emacsos-assist-web-new-thread= |
| Send from an Assist Web thread | =emacsos-assist-web-send= |
| Refresh an Assist Web thread | =emacsos-assist-web-refresh-thread= |
| Refresh the Assist Web thread list | =emacsos-assist-web-refresh-threads= |
| Conversation actions available in this buffer | =emacsos-conversation-command= |
| Load older Assist Web messages | =emacsos-assist-web-load-older= |
| Stop or detach from an Assist Web response | =emacsos-assist-web-abort= |
| Start a file-backed chat | =emacsos-assist-new-file= |
| Reload an unchanged file-backed chat from disk without prompting | =emacsos-assist-refresh= |
| Forget a file-backed chat checkpoint | =emacsos-assist-forget= |
| Compose an SMS | =emacsos-send-message= |
| Open SMS conversations | =emacsos-sms-chat-catalog= |
| Open one SMS conversation | =emacsos-sms-chat-open= |
| Refresh SMS history | =emacsos-sms-chat-refresh= |

The global =C-c a= Assist prefix and =C-c e= EmacsOS prefix work in every
buffer.  On the touchscreen, swipe up from =c= on the keyboard, then tap =a=
or =e=, to type either prefix.

| Key | Action |
|-----+--------|
| =C-c a l= | Browse canonical threads |
| =C-c a t= | Open a canonical thread by completion |
| =C-c a n= | New canonical thread |
| =C-c a r= | Refresh thread list |
| =C-c a f= | New .assist file |
| =C-c e c= | Ordinary chat |
| =C-c e d= | Stage call |
| =C-c e m= | Open SMS conversations |
| =C-c e s= | Stage SMS |
| =C-c e w= | Network controls |
| =C-c e h= | This command reference |

** PinePhone/OpenRC-only bindings

These bindings are installed only by the PinePhone/OpenRC deployment;
the generic =phone-install= target does not define them.

| Key | Action |
|-----+--------|
| =C-c e k= | Hide or show the PinePhone keyboard |
| =C-c e b= | Start or switch to Firefox on PinePhone |
| =C-c e a= | Start or switch to Android on PinePhone |

** Conversation bindings

| Key | Action |
|-----+--------|
| =C-<return>= | Send |
| =C-c C-r= | Refresh current conversation |
| =C-c C-k= | Abort or detach |
| =C-c C-o= | Open the HTTP(S) object at point |
| =C-c C-l= | Load older canonical history |
| =C-c C-f= | Forget a file-backed conversation |

RET accepts an active minibuffer first.  Otherwise touchscreen RET invokes the
target buffer's physical =RET= binding: in a conversation it opens a literal
HTTP(S) object at point or inserts a newline, and in the native thread list it
opens the exact row at point.

* Phone and network

| Action | M-x command or =eval_elisp= form |
|--------+-----------------------------------|
| Stage a call for confirmation | =emacsos-call= |
| Answer the ringing call | =emacsos-answer= |
| Hang up | =emacsos-hang-up= |
| Open phone controls | =emacsos-controls-show= |
| Set cellular data explicitly | =(emacsos-net-set-cell t)= or =(emacsos-net-set-cell nil)= |
| Set brightness to 25, 50, 75, or 100 | =(emacsos-controls-set-brightness 50)= |
| Set flashlight on or off | =(emacsos-controls-set-flashlight 'on)= or =(emacsos-controls-set-flashlight 'off)= |
| Open network controls | =emacsos-net-show= |
| Connect to a visible Wi-Fi network | =emacsos-net-connect= |
| Set Wi-Fi explicitly | =(emacsos-net-set-wifi t)= or =(emacsos-net-set-wifi nil)= |
| Toggle Wi-Fi | =emacsos-net-toggle-wifi= |
| Toggle cellular data | =emacsos-net-toggle-cell= |
| Hide or show the keyboard | =emacsos-pinephone-toggle-keyboard= |

Parenthesized entries are =eval_elisp= forms.  Bare command names run through
=M-x=.

The transient call and SMS modeline labels are touch controls for their current
status or action.  The primary platform label opens Network controls on generic
systems and the combined Controls buffer on PinePhone/OpenRC.  Assist threads
open through =C-c a l= or =C-c a t=.  Keyboard hide/show and device state live in the
Controls buffer.

* Swipe learning (PinePhone/OpenRC only)

These commands are installed by the PinePhone/OpenRC image.  Swipe learning is
off by default and never changes the current keyboard's suggestions.  It retains
bounded local trace geometry, candidate words, and your explicit correction
outcomes.  It does not capture surrounding text from Emacs, Firefox, Android,
or another application.

| Action | M-x command |
|--------+-------------|
| Enable capture for the next UI session | =emacsos-swipe-learning-enable= |
| Disable persistence now | =emacsos-swipe-learning-disable= |
| Show bounded local evidence | =emacsos-swipe-learning-show= |
| Export a private local snapshot | =emacsos-swipe-learning-export= |
| Erase all learning evidence | =emacsos-swipe-learning-erase= |
| Restart the supervised phone UI | =emacsos-pinephone-restart-ui-session= |

After Enable, restart the UI to begin capture.  Disable fences all future
persistence immediately; restart the UI to stop keyboard observation.  Erase
requires =Disable → restart UI → erase= and a second tap in its confirmation
buffer.  Restart closes transient buffers and refuses while a file-visiting
buffer has unsaved edits.

An SMS conversation uses the compositor text keyboard with QUIT, Refresh, and
Send or ACK in its Emacs utility row.  Send still opens the separate two-tap
confirmation screen; ACK clears an indeterminate local send status.  Other
ordinary chat, editing, and Assist buffers have no separate Emacs control pane.

* Files and terminals

| Action | Command or key |
|--------+----------------|
| Open a file | =M-x find-file= |
| Switch buffer | =M-x switch-to-buffer= |
| Save | =M-x save-buffer= |
| Undo | =M-x undo= |
| Open or choose a persistent terminal | =M-x dtach-shell= or =C-c d= |
| Reconnect the current persistent terminal | =M-x dtach-shell-reconnect= or =C-c S-d= |

* PinePhone/OpenRC hardware

| Control | Action |
|---------+--------|
| Power button | Blank the display; press again to wake |
| Volume button | Wake the display |
| =Alt+Tab= | Move to the next full-screen application |

When the custom modifier-swipe keyboard is active, swipe upward from a character
for =Control+character= and downward for =Meta+character=.
